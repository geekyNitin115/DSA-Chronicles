#include <iostream>
using namespace std;
struct Node{
    int num;
    string str;
    double doub;
    char x;

    // Node(num_,str_,doub_,x_){
     // Constructor with explicit parameter types
    Node(int num_, string str_, double doub_, char x_)
        : num(num_), str(str_), doub(doub_), x(x_) {}
    //     num: num_ ;
    //     str:str_;
    //     doub :doub_;
    //     x: x_;
    // }

    /* data */
};

int main(){

    // METHOD -1

    // node nitin = new node("Nitin",79,91.0,"");
    // cout<<nitin.str;

   // METHOD-2
//    Node nitin(79, "Nitin", 91.0, ' '); // Using a space character for `x`

//         // Correctly access `str` member of `nitin`
//         cout << nitin.str;
//         return 0;
  
  array<int,4>
     
}